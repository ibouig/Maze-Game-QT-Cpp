#ifndef MENU_H
#define MENU_H

#include <QWidget>

class menu : public QWidget
{
    Q_OBJECT
public:
    explicit menu(QWidget *parent = 0);
    ~menu();

signals:

public slots:
};

#endif // MENU_H
